/** The dashboard route path, defined once so the shell's navigation and the landing redirect agree. */
export const DASHBOARD_ROUTE = '/dashboard';

/**
 * The query parameter naming the selected Branch on the dashboard (manual-review Correction 3) and
 * carried over when linking into the Alerts list, so the same string is never respelled differently
 * in two places.
 */
export const BRANCH_ID_QUERY_PARAM = 'branchId';
