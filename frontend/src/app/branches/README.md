# `branches`

Branch management feature (IP-01 §3; FS-02 Increment A).

Planned contents (implemented in later tasks, **not** in the T-22 scaffold):

- `BranchListComponent`, `BranchCreateComponent`, `BranchDetailComponent` (T-26, T-27).
- `CameraConfigFormComponent`, `ActivationKeyDisplayComponent`, `DeviceStatusBadgeComponent`
  (T-27, T-28, T-29).
- `BranchService`, `DeviceService` (Angular API clients).

This folder currently only establishes the module boundary; no branch/device UI or API integration is
implemented at the scaffold stage.
