# `auth`

Authentication feature (IP-01 §3; FS-01).

Planned contents (implemented in later tasks, **not** in the T-22 scaffold):

- `LoginComponent` — the Admin login view (T-23).
- `AuthService` — login/logout calls and token storage (T-23).

This folder currently only establishes the module boundary; no login view or authentication logic is
implemented at the scaffold stage.
