# .NET build artifacts
[Bb]in/
[Oo]bj/
*.user

# Angular / Node (added when frontend is scaffolded)
node_modules/
dist/

# Environment / secrets
*.env
.env.*

# Local MCP server config — contains an API key; must never be committed.
mcp.json
.mcp.json
